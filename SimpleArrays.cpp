#include <stdio.h>

int main() {
    int num_arr[] = {25, 50, 75, 100};

    
    for (int i = 0; i < 4; ++i) {
        printf("%d ", num_arr);   // Print all elements in one line
    }
    printf("\n");                    // Add a newline at the end

    return 0;
    // a return value of 0 indicates successful execution of the program.

    // printf("%d", num_arr);
}
